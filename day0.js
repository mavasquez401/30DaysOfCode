solve = (mealCost, tip, tax) => {
    let tipPercent = (tip * mealCost)/100
    let taxPercent = (tax * mealCost)/100
    console.log(mealCost + tipPercent + taxPercent);
    
    return 
}

solve(100, 15, 8);

